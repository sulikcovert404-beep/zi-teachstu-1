# پلتفرم آموزش هوشمند ایران — خلاصهٔ کامل پروژه

> سند خلاصهٔ کل پروژه (راند ۲۲ — سپتامبر ۲۰۲۶). این فایل مرجع سریع مدیر، توسعه‌دهنده و QA است.

---

## ۱) معرفی

**پلتفرم آموزش هوشمند ایران** یک سامانهٔ چند-مستأجری (Multi-Tenant) و چند-کاناله (Omnichannel) آموزشی با رابط کاملاً فارسی و راست‌به‌چپ است که برای مدارس ایران طراحی شده و در **سه کانال** ارائه می‌شود:

| کانال | نشانی | توضیح |
|---|---|---|
| 🌐 وب | `/` (Next.js - port 3000) | اپ تک‌صفحه‌ای با داشبورد مخصوص هر نقش |
| 🤖 بات تلگرام | [@teachstu2026_bot](https://t.me/teachstu2026_bot) (port 3003) | بات کامل با آپلود PDF، آزمون تعاملی، ارسال فایل |
| 📱 مینی‌اپ تلگرام | همان اپ وب داخل WebApp تلگرام | ورود خودکار با initData + دکمهٔ «باز کردن در مرورگر» |

**ایدهٔ مرکزی:** مدیر/معلم یک **کتاب درسی** (فایل PDF یا لینک دانلود) بارگذاری می‌کند؛ هوش مصنوعی به‌صورت خودکار **۵ مصنوعهٔ آموزشی** می‌سازد: خلاصه، جزوه، نمونه‌سؤال (۴ مدل)، شکل‌های آموزشی SVG و پادکست صوتی — و دانش‌آموز در وب و تلگرام با آن‌ها درس می‌خواند، آزمون می‌دهد و امتیاز جمع می‌کند.

---

## ۲) نقش‌ها و حساب‌های نمایشی

| نقش | ایمیل | رمز | توان اصلی |
|---|---|---|---|
| مدیر کل پلتفرم (SUPER_ADMIN) | `owner@platform.ir` | `123456` | همه‌چیز + تأیید کتاب مدارس + پیکربندی بات/AI |
| مدیر مدرسه (SCHOOL_ADMIN) | `admin@school.ir` | `123456` | مدیریت مدرسه (پس از فعال‌شدن مجوزها) |
| معلم (TEACHER) | `teacher@school.ir` | `123456` | کلاس، آزمون، جزوه، بارگذاری کتاب (با مجوز) |
| دانش‌آموز (STUDENT) | `student@school.ir` | `123456` | کتاب‌خانه، آزمون، دستیار، اشتراک PRO فعال |

ساختار درسی مطابق سایت [chap.sch.ir](http://www.chap.sch.ir/) (سازمان کتاب‌های درسی): **دوره (پیش‌دبستانی/ابتدایی/متوسطهٔ اول/متوسطهٔ دوم/هنرستان) → پایه → درس → کتاب** — در وب با فیلتر تراشه‌ای (chip cascade) و در بات با ویزارد ۴ مرحله‌ای.

---

## ۳) معماری و پشتهٔ فنی

```
Next.js 16 (App Router) + TypeScript 5 + Tailwind CSS 4 + shadcn/ui (New York)
Prisma ORM + SQLite (db/custom.db) — ۳۵ مدل داده
احراز هویت: نشست opaque (توکن هش‌شده در DB) + Bearer/Cookie + ExternalIdentity برای تلگرام
دروازهٔ مرکزی AI: src/server/ai/gateway.ts (همهٔ فراخوانی‌های AI از همینجا + متر)
  ├─ موتور پیش‌فرض: z-ai (LLM/TTS) با سهمیه‌بندی و ثبت مصرف
  └─ موتور اختیاری: Gemini با کلید شخصی مدیر (کلید فقط سمت سرور ذخیره می‌شود)
بات تلگرام: mini-services/telegram-bot (Bun + long-polling، صف سریالی per-chat، watchdog)
خروجی PDF: موتور رندر Chromium + فونت Vazirmatn جاسازی‌شده (src/server/services/pdf-export.ts)
```

**خط تولید محتوای کتاب (async pipeline):** آپلود/لینک → استخراج متن RTL از PDF → ساختاردهی (دوره/پایه/درس) → تولید موازی ۵ مصنوعه از طریق AI Gateway → وضعیت لحظه‌ای (PENDING/GENERATING/READY/FAILED + بازتولید تک‌مصنوعه).

---

## ۴) قابلیت‌های کلیدی (وضعیت فعلی)

### 🌐 وب
- **داشبورد مدیر کل:** نمای کلی آماری، سازمان‌ها، کاربران، کتاب‌خانه (آپلود فایل **یا** لینک دانلود + «تست لینک» واقعی با خطای فارسی)، صف تأیید کتاب مدارس/معلمان، پلن‌ها، مصرف AI، ارائه‌دهنده‌های AI (Gemini)، تنظیمات و اتصال‌ها (توکن بات + آدرس مینی‌اپ + پیکربندی خودکار)، فلگ‌ها، ردیابی، **پیش‌نمایش امن نقش‌ها**.
- **کتاب‌خانهٔ هوشمند (دانش‌آموز/معلم):** مرور به سبک chap.sch.ir (تراشه‌های دوره→پایه→درس + جست‌وجو)، کارت کتاب با وضعیت ۵ مصنوعه، جزئیات کتاب با خلاصه/جزوه/شکل‌ها/پخش پادکست/آزمون تعاملی ۵ مدلی با تصحیح خودکار و امتیاز.
- **دانلودها:** «دانلود PDF» (خلاصه/جزوه/نمونه‌سؤال هر مدل) با **فونت فارسی وزیرمتن و چارچوب رسمی برگهٔ آزمون + پاسخ‌نامهٔ تشریحی** + نسخهٔ Word + چاپ + PDF اصلی خود کتاب.
- **ابزارهای دانش‌آموز/معلم:** دستیار هوشمند (RAG)، پادکست‌ساز، پرسش‌وپاسخ منبع، جزوه، آزمون‌ساز، کارت‌های یادآور، برنامهٔ مطالعه، اشتراک/صورت‌حساب.

### 🤖 بات تلگرام (@teachstu2026_bot)
- ورود دوطرفه: **📱 اشتراک شمارهٔ موبایل (ورود خودکار اگر با شمارهٔ حساب وب یکی باشد)** یا 🔗 کد ۶ رقمی؛ خروج/قطع اتصال در هر لحظه.
- کتاب‌خانه با فیلتر دوره، کارت کتاب، ارسال خلاصه/جزوه به‌صورت متن + **فایل PDF با فونت فارسی**، **نمونه‌سؤال PDF** (برگهٔ آزمون + پاسخ‌نامه)، پادکست صوتی (sendAudio) با دکمهٔ **«📥 ذخیره در پیام‌های ذخیره»**، دانلود PDF اصلی کتاب، **آزمون تعاملی با دکمه‌های شماره‌گذاری‌شده و تصحیح سرور-ساید + امتیاز**.
- **آپلود کتاب از خود تلگرام:** ارسال فایل PDF به بات → دانلود از تلگرام → استخراج متن → ویزارد دوره→پایه→درس→عنوان → ثبت + خط تولید کامل (معلم/مدارس نیازمند تأیید مدیر کل).
- ثبت‌نام داخل بات/مینی‌اپ با شماره و انتخاب مدرسه + جریان تأیید توسط مدیر.

### 📱 مینی‌اپ
- ورود خودکار با initData امضاشده؛ در حالت متصل‌نشده بنر «ورود با ایمیل/رمز» + **«📱 ورود با شمارهٔ تلفن»** (requestContact) + اتصال خودکار پس از ورود.
- رابط کامل وب (ریسپانسیو، تم روشن/تاریک) + «باز کردن در مرورگر» با توکن یک‌بارمصرف.

### 🔐 اتصال تلگرام (خواستهٔ راند ۲۲)
- کاربر در وب (دیالوگ «اتصال حساب به تلگرام») **شمارهٔ موبایلش را ثبت می‌کند** (`PUT /api/v1/me/phone`) → در بات/مینی‌اپ همان شماره را به اشتراک می‌گذارد → **بدون رمز و بدون کد، مستقیم وارد همان حساب می‌شود** (مالکیت شماره توسط خود تلگرام تأیید می‌شود؛ پیوند قبلی به کاربر دیگر منتقل می‌شود).

---

## ۵) نقشهٔ API (خلاصه)

```
POST /api/v1/auth/login|logout|me            GET/PUT /api/v1/me/points|phone
POST /api/v1/auth/telegram                    (initData → session)
POST /api/v1/auth/telegram/link|link-code|unlink|link-phone
POST /api/v1/internal/telegram/{config|link|link-phone|resolve|signup|unlink|web-login}  (x-bot-secret)
GET/POST /api/v1/books…                       (list/detail/create/delete/approve/regenerate)
POST /api/v1/books/extract-pdf|extract-url|test-url   (SSRF-guarded)
GET  /api/v1/books/{id}/summary.pdf|notes.pdf|quiz.pdf?model=   ← موتور PDF فارسی
GET  /api/v1/books/{id}/summary.docx|notes.docx|original.pdf|podcast|quiz
POST /api/v1/books/{id}/quiz/attempts         GET /api/v1/platform/overview …
```

کد خطاها همیشه `{ error: { code, message } }` با پیام فارسی است.

---

## ۶) اجرا و عملیات

```bash
bun run dev            # اپ Next.js — port 3000 (لاگ: dev.log)
cd mini-services/telegram-bot && bun run dev   # بات — port 3003 (bun --hot)
bun run db:push        # اعمال schema پرایسما
bun run lint           # کیفیت کد
```
- دیتابیس: `db/custom.db` · فونت PDF: `public/fonts/Vazirmatn-*.woff2`
- **ذخیره‌سازی فایل‌ها (راند ۲۳): «کلا هیچ چیز در هاست»** — همهٔ باینری‌های کتاب (PDF اصلی، پادکست WAV، خلاصه/جزوه/نمونه‌سؤال PDF) یک بار به چت ذخیره‌سازی تلگرام (پیش‌فرض: چت خصوصی مدیر کل با بات) آپلود و `file_id` دائمی‌شان در جدول `TelegramAsset` ثبت می‌شود؛ نسخهٔ محلی بلافاصله حذف می‌شود. دانلودها (وب و بات) مستقیماً از تلگرام سرو می‌شوند: بات با file_id (لحظه‌ای)، وب با پروکسی getFile (تا ۲۰MB؛ بزرگ‌ترها با لینک عمیق خود بات). سقف آپلود هر فایل ۵۰ مگابایت.
- QA خودکار: هر ۱۵ دقیقه cron «webDevReview» (ارزیابی وضعیت + agent-browser + ادامهٔ توسعه).

---

## ۷) وضعیت فعلی و اولویت‌های بعدی

**وضعیت (راند ۲۵):**
- **فهرست زندهٔ مدل‌های جمینای (راند ۲۵):** مدل‌ها مستقیماً از API گوگل گرفته می‌شوند و در تنظیمات قابل انتخاب‌اند — ListModels رسمی، و اگر گوگل آن را برای موقعیت سرور بسته باشد «آزمون مستقیم» هر مدل (countTokens رایگان + دنبال‌کردن جایگزین‌های پیشنهادی گوگل). تأیید زنده: ۲.۵‌ها بازنشسته‌اند؛ کاتالوگ فعلی: gemini-3.6-flash / gemini-3.5-flash-lite / gemini-3.1-pro-preview + مستعارهای «همیشه جدیدترین». اعتبارسنجی مدل قالب-محور شد و پیش‌فرض gemini-flash-latest است.
- **⚠️ محدودیت جغرافیایی گوگل:** کلید BYO مدیر معتبر است اما گوگل موقعیت IP این میزبان را در ListModels و (اکثر) generateContent مسدود می‌کند («User location is not supported») — با تغییر کلید/مدل رفع نمی‌شود؛ پیام فارسی دقیق در «آزمودن اتصال» و کارت مدل نمایش داده می‌شود. ارائه‌دهندهٔ پیش‌فرض zai کاملاً سالم است.
- **خودترمیمی (راند ۲۴):** ورود وب هرگز نمی‌شکند (ensureDemoData در مسیر لاگین اگر DB خالی باشد حساب‌های نمونه را بازمی‌سازد) و رازهای حیاتی (توکن بات/کلید جمینای) در آینهٔ فایلی db/settings-backup.json باقی می‌مانند و پس از پاک‌شدن DB خودکار بازیابی می‌شوند.
- معماری «ذخیره‌سازی کامل در تلگرام» (راند ۲۳) فعال و E2E-تست‌شده (کارت ☁️ در تنظیمات، دکمهٔ انتقال، پروکسی getFile تا ۲۰MB). راند ۲۲: PDF فارسی وزیرمتن، ورود با شماره.
- بات تلگرام احیا شد (توکن جدید مدیر ذخیره شده — @teachstu2026_bot).

**باقی‌مانده (پیشنهاد اولویت):**
0. تست دستی مدیر در تلگرام واقعی (/start، پادکست/PDF لحظه‌ای با file_id، ☁️ در کارت کتاب) + بازسازی کتاب‌های نمونه (DB راند-۲۴ خالی شد؛ زیرساخت آماده است).
1. نقش والد (PARENT) و داشبورد مربیان.
2. سخت‌سازی Milestone I: rate-limit عمومی، صفحه‌بندی فهرست‌ها، پشتیبان‌گیری دوره‌ای DB.
3. جدول امتیازها/لیدربورد مدرسه‌ای و نشان‌ها.
4. دکمهٔ PDF مدل‌های دیگر نمونه‌سؤال در بات (فعلاً MC) + گزینهٔ «همهٔ مدل‌ها».
5. کانال بله (Bale) با همان الگوی تلگرام.

> جزئیات فنی هر راند در `worklog.md` (سند تحویل/اقدام) و اسکرین‌شات‌های QA در `download/` موجود است.
